# 雲纖醫境 YUN XIAN — 代謝美學診所官網

## Overview
零依賴的 Node.js 靜態網站伺服器（`server.js`），前端檔案在 `public/`，站台文案在 `data/site.json`（改完重新整理即可生效），預約資料寫入 `data/bookings.jsonl`。

## How to run
- 工作流「Start application」：`PORT=5000 node server.js`（無需安裝任何套件）

## Notes
- 匯入時本地與 GitHub 歷史不相關，已用 `--allow-unrelated-histories` 合併 origin/main（第一版）。
