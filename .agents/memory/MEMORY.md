# Memory Index

- [Site conventions](site-conventions.md) — bump `styles.css?v=N` in index.html AND knowledge.html on every CSS change; avoid `:has()` (use explicit state classes); AI interior images must prompt "no text/signage" or they invent fake clinic names.
- [Public form abuse controls](public-form-abuse-controls.md) — Replit proxy IP headers are not a trusted per-client identity; use bounded global safeguards unless verified edge bot control is configured.
